export declare class AutomationService {
    /**
     * Identifies all students with outstanding balances across all schools
     * and sends reminders to parents/admins.
     */
    static runWeeklyDefaulterReminders(): Promise<void>;
}
